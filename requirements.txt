speechmatics-rt
PyAudio
python-dotenv
pytest

# Post-processing engine: Aho-Corasick automaton.
# pyahocorasick is a lean C extension with Windows/macOS/Linux wheels, unlike
# the old OpenFst/Pynini stack which shipped Linux-only wheels and needed a
# full C++ toolchain elsewhere. Pinned to the version the parity tests and
# benchmarks run against. If the package is unavailable the layer falls back
# to a built-in pure-Python automaton with identical output
# (`MedicalFST.uses_ahocorasick` reports which backend is active).
pyahocorasick==2.3.1

# Non-Windows fallback injection (pyautogui hotkeys + pyperclip clipboard).
# The Windows path uses the native ctypes implementation and needs neither.
# Declared because create_injector() only imports them lazily at the first
# paste, which made a clean Linux/macOS install start happily and then fail
# every single paste at runtime.
pyautogui
pyperclip

# NOTE: python-bidi is no longer used. The application keeps all text in
# LOGICAL Unicode order and controls direction with explicit Unicode controls
# (RLM + RLE ... PDF, see speechmatics_test/presentation.py). Pre-shaping to
# visual order double-applied the BiDi algorithm, because Tk and the target
# editors already run it themselves.
