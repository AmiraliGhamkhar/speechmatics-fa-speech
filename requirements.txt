speechmatics-rt==1.1.1
PyAudio==0.2.14
python-dotenv==1.2.3
pytest>=8.0

# Post-processing engine: Aho-Corasick automaton.
# pyahocorasick is a lean C extension with Windows/macOS/Linux wheels, unlike
# the old OpenFst/Pynini stack which shipped Linux-only wheels and needed a
# full C++ toolchain elsewhere. If the wheel is unavailable the layer falls
# back to a built-in pure-Python automaton with identical output
# (`MedicalFST.uses_ahocorasick` reports which backend is active).
pyahocorasick>=2.0.0

# python-bidi: correct visual-order shaping of mixed Persian/English text in
# the overlay (Tk's own BiDi shaping mis-orders mixed sentences on some
# platforms). The app degrades gracefully when it is missing.
python-bidi>=0.6.0
